# UltiCleaner — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`. **This repository does NOT share a dispatch batch with any
> other repository.** Its four scheduled rows each impose a real wait — every one of them lowers
> its own interval config key below the shipped default first, and even so the four rows together
> impose approximately 35 seconds of pure scheduled-wait time (15s + 15s + 5s + 0s — see
> each row's own Preconditions for the exact key and lowered value), on top of the setup time each
> row's Preconditions need (spawning dropped items, spawning configured-type mobs, and lowering the
> interval key each scheduled row names). Dispatching this
> repository alongside another would either force the other repository's rows to share that same
> wait window for no benefit, or force this repository's waits to serialize behind unrelated rows
> — plan 10-16 schedules `UltiCleaner` alone for exactly this reason. The **two** rows under
> `## Lifecycle Hooks` add more on top of those 35: `ulticleaner.lifecycle.reload` adds roughly 25
> seconds, plus up to about 40 more if its step 1 has to wait for the item countdown to restart; and
> `ulticleaner.lifecycle.removed-key-warning` adds **two full server stop/start cycles**, which is
> wall-clock time this document cannot put a number on because it depends on the host — budget it as
> two restarts, not as seconds. `ulticleaner.clean.status` adds three 65-second waits (two at a
> lowered vanilla `/tick rate`, one after restoring it), and during the first two every tick-based
> timer on the server runs slow, so no other timed row may overlap it.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through
  the maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions
  phrase `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer
  `human`. This repository has no such row — it has no panel-capability surface of its own — so
  none is currently affected; the convention is stated here for template consistency.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works". Where `FEATURES.md` documents a key or task
  as having no observable effect (`tps-fallback` on Paper), the corresponding
  Expected states that absence explicitly, as its own observable claim, rather than being silently
  skipped. `messages.prefix` used to be the other example here; it was deleted rather than wired
  (`UltiKits/UltiCleaner#18`), so it is no longer a key with no effect — it is no longer a key.
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. This repository has NO entry in `.planning/phases/09-module-ecosystem-readiness-and-
  test-coverage/gui-exclusions/` at all (it ships no GUI page of any generation), so every row's
  `Covers` cell below is blank — a structural fact, not an oversight.
- A row whose Preconditions name a prior CHECKLIST row must appear after that row in file order —
  asserted mechanically: for every row, every checklist ID cited in its Preconditions cell must
  have a strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never
  one row per key. This module ships exactly one `@ConfigEntity` (`CleanerConfig`,
  `config/cleaner.yml`), so exactly one config row exists below (ID suffixed `-yml`,
  `ulticleaner.config.cleaner-yml`). This module ships no Maven-filtered (build-time) config file,
  so the build-time-property clause of this rule does not apply to any row below.
- **Language:** every chat and console line this module writes follows the framework's `language`
  setting (`UltiKits/UltiCleaner#17`). Every row below whose Expected quotes a line carries the
  precondition `language: en` set in `plugins/UltiTools/config.yml` and quotes the English text
  exactly (colour codes rendered). The seven broadcast messages are written into
  `config/cleaner.yml` in the server's language while they are still built-in text, and the module
  broadcasts exactly what the file holds; a row that quotes a broadcast requires those values to be the
  text this build writes under `language: en`. Before the session, `plugins/UltiTools/pluginConfig/UltiCleaner/lang/`
  is moved aside so this jar's catalogues are extracted fresh (an upgraded install never refreshes an
  already-extracted language file, `UltiKits/UltiTools-Reborn#459`; `ulticleaner.i18n.language`).
- **Config text rows:** `ulticleaner.config.cleaner.materialize-fresh` and `ulticleaner.config.cleaner.materialize-switch`
  exercise `FEATURES.md`'s `ulticleaner.lifecycle.legacy-message-defaults` on a fresh file and across a `language`
  switch; they are named by the phase's config-text convention (`<prefix>.config.<file stem>.materialize-*`)
  rather than by that ID, and the upgrade case keeps the existing row `ulticleaner.lifecycle.legacy-message-defaults`.
- **Scheduled-row wait discipline:** every scheduled row's Preconditions name the exact
  `config/cleaner.yml` key that shortens its interval below the shipped default, and the value to
  set it to, so the executor never has to guess a wait. A row with no stated wait would be a row
  the executor cannot run without guessing, and a guessed wait produces a false `blocked` — see
  the plan's own instruction for this repository.

## Commands

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ulticleaner.clean.items | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean`; at least 3 distinguishable dropped item stacks on the ground in a non-blacklisted world, each older than `item.ignore-recent` seconds (30, shipped default — wait that long after dropping, or lower the key first), none a whitelisted material (`item.whitelist`'s shipped default: DIAMOND, EMERALD, NETHER_STAR, BEACON, ELYTRA), none carrying a custom display name; no cleanup already in progress | Run `/clean items` | Chat line reads "Started cleaning N ground items (processing in batches)..." (green) where N is at least 3; within `batch.size` (50, shipped default) ticks — effectively immediate for 3 items — all 3 stacks are removed from the world and a chat line reads "[Cleaner] Cleaned N ground items!" (green) | server | |
| ulticleaner.clean.items.neg-in-progress | `language: en` in the framework's `config.yml`; A large cleanup is already in progress, set up with ALL three of: (a) `batch.size: 10` in `config/cleaner.yml` (default 50, so the window a small population gives is only ~1 tick — too narrow for a human or real-client executor to reliably hit), set either before the server starts or on the running server followed by `/ul reload UltiCleaner` (which re-reads the file since `UltiKits/UltiCleaner#14`); (b) at least 200 non-whitelisted, unnamed, aged dropped items, giving roughly 20 ticks (1 second) of processing time at `batch.size: 10`; (c) `/clean items` run once, then immediately proceed to the next step | Run `/clean items` again while the first batch is still processing | Chat line reads "Cleaning in progress, please wait..." (yellow); no second batch starts, no double-counting occurs | server | |
| ulticleaner.clean.entities | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean`; at least 3 entities of a configured type (`entity.types`' shipped default includes ZOMBIE) present in a non-blacklisted world, none custom-named, none leashed, none tamed; no cleanup already in progress | Run `/clean entities` | Chat line reads "Started cleaning N entities (processing in batches)..." (green) where N is at least 3; within a few ticks all 3 entities are removed from the world and a chat line reads "[Cleaner] Cleaned N entities!" (green) | server | |
| ulticleaner.clean.entities.neg-in-progress | `language: en` in the framework's `config.yml`; A large cleanup is already in progress (same setup as `.items.neg-in-progress` above — `batch.size: 10`, at least 200 eligible entities — using entities instead of items) | Run `/clean entities` while the first batch is still processing | Chat line reads "Cleaning in progress, please wait..." (yellow); no second batch starts | server | |
| ulticleaner.clean.all | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean`; at least 1 eligible dropped item AND at least 1 eligible entity present (same eligibility rules as `.items`/`.entities` above); no cleanup already in progress | Run `/clean all` | Chat line reads "Started cleaning M items and N entities (processing in batches)..." (green) with both M and N at least 1; the item(s) ARE removed from the world; the entity/entities are NOT removed — `isCleaningInProgress` was already `true` (set synchronously by the just-started item batch) by the time `forceCleanEntities()` ran in the same tick, so `cleanEntitiesWithBatch` returned immediately with no broadcast and no removal, even though N was reported as started. Known product defect, `UltiKits/UltiCleaner#15` | server | |
| ulticleaner.clean.check | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean`; at least one ground item and one configured-type mob present server-wide, so the printed counts are non-trivial | Run `/clean check` | Chat prints "=== Server Entity Statistics ===" (gold) followed by "Ground items: N" (N matching a manual count), "Cleanable mobs: M" (M matching a manual count of configured types only, regardless of whitelist exemptions), "Total entities: T" (T at least N+M), then exactly one chunk line, "Loaded chunks: C" (C a positive integer, printed unconditionally and no longer gated on any configuration key — it is a plain server statistic, not a cleanup readout; every loaded world holds at least its own spawn-keep chunks, so a C of 0 on a running server is itself a finding, and C must also not equal the "Total entities" figure above it, which would indicate the two readouts were transposed), then a final line "Server TPS: R (Band)" with the current reading, e.g. "Server TPS: 20.00 (Normal)". NO "Unloadable chunks" line appears: that readout went with the removed chunk-unload feature (`UltiKits/UltiCleaner#27`) | server | |
| ulticleaner.clean.status | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean`; the scheduled item and entity cleanup tasks are both enabled (shipped default); `tps.low-reduction: 60` and `tps.critical-reduction: 70` set before the server starts, both deliberately NOT the shipped 30 and 50 — a run left at the defaults cannot tell an interpolated value from the literal this row exists to prove is gone; `tps.low-threshold` and `tps.critical-threshold` at their shipped 18.0 and 15.0, and this row does not change them; `tps.adaptive-enabled: true` and `tps.sample-window: 1m` (shipped defaults); from the console, `/minecraft:tick query` reports a target tick rate of 20; no other timing-sensitive row is running on this server — steps 2 and 3 slow every tick-based timer server-wide, this module's own countdowns included | 1. On an idle server, run `/clean status` and read the output. 2. From the console, run `/minecraft:tick rate 16`. Wait 65 seconds, then run `/clean status` once and read the output. 3. From the console, run `/minecraft:tick rate 10`. Wait 65 seconds, then run `/clean status` once and read the output. 4. Cleanup, whatever the outcome: from the console, run `/minecraft:tick rate 20` and confirm `/minecraft:tick query` reports a target of 20, then wait 65 seconds before starting any other timed row or any row that reads a TPS figure. Run `/clean status` exactly once per step and never more often than once every 10 seconds, and run every `/minecraft:tick` command from the console, not from the client: an earlier run of this row polled `/clean status` from the client often enough to be kicked for spamming, which ended its observation. The 65-second wait exists because Paper's one-minute TPS figure (the one `tps.sample-window: 1m` reads) is the number of ticks in the last 60 seconds divided by the wall time they took, so it equals a new fixed tick rate once 60 seconds have passed at it. That relation is worked out from the Paper server's bytecode and has not yet been observed on a live server, so this row checks it itself: if the TPS number printed at step 2 is not between 15.50 and 16.50, or the one printed at step 3 is not between 9.50 and 10.50, the fixture did not produce the rate it set — record `blocked` with that step's `/clean status` output and the `/minecraft:tick query` output, and do not record `pass` on a band that was not reached. A number inside its range with a band word or warning line that differs from Expected is the module's own result and is judged against Expected as usual | Step 1: chat prints "=== Cleanup Status ===" (gold), then "Next item cleanup: S1 seconds" and "Next entity cleanup: S2 seconds" (both counting down from their own configured interval), then "Cleanup status: Idle" (gray) when no batch is running or "In progress..." (green) when one is, then the TPS line reading "(Normal)" — an idle server is at or above the shipped 18.0 threshold — and NO warning line after it. Step 2: the TPS line reads about 16 followed by "(Low)" (below the shipped 18.0, not below 15.0), and is followed by exactly one further yellow warning line whose text ends "60%" — the configured `tps.low-reduction` — and NOT "30%". Step 3: the TPS line reads about 10 followed by "(Critical)" (below the shipped 15.0), and is followed by exactly one further red warning line whose text ends "70%" — the configured `tps.critical-reduction` — and NOT "50%", with no yellow line beside it (the critical branch replaces the low one, it does not add to it). Fixed, `UltiKits/UltiCleaner#21` | server | |
| ulticleaner.clean.help | `language: en` in the framework's `config.yml`; Sender holds `ulticleaner.clean` | Run bare `/clean` (no arguments) | Chat prints "=== UltiCleaner Help ===" (gold) followed by exactly five sub-command lines (items/entities/all/check/status), in this order: "/clean items - Clean ground items", "/clean entities - Clean entities", "/clean all - Clean everything", "/clean check - View entity statistics", "/clean status - View cleanup status". NO `/clean chunks` line appears — that sub-command was removed with the chunk-unload feature (`UltiKits/UltiCleaner#27`) | server | |

## Automatic Cleanup

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ulticleaner.scheduled.item-tick | `language: en` in the framework's `config.yml`, and the seven `messages.*` keys in `config/cleaner.yml` holding the English text this build writes under `language: en` (a fresh file, or an upgraded file whose old Chinese defaults start-up has rewritten); `item.enabled: true` (shipped default); `item.interval: 15` (NOT the shipped default of 300 — lowered for testability, still above the highest configured `item.warn-times` entry that will fire, 10); `item.warn-times` at its shipped default (60, 30, 10, 5, 3, 2, 1); `entity.interval` at its shipped default (600) or otherwise NOT lowered to a value that could reach zero during this row's own 15-second wait — a same-tick collision between item and entity cleanup silently drops one of them via the shared `isCleaningInProgress` flag, `UltiKits/UltiCleaner#15`; at least one eligible dropped item present (same eligibility as `ulticleaner.clean.items` above), placed BEFORE the countdown reaches 10; known framework defect outside this change, `UltiKits/UltiTools-Reborn#523` (module symptom `UltiKits/UltiCleaner#22`): the framework's config parser binds every element of `item.warn-times` as a string while the module looks each countdown mark up as an integer, so on a boot that reads the key from an existing `config/cleaner.yml` no countdown warning is ever broadcast. Until #523 is fixed, a run in which the item(s) are removed at 0 seconds but the countdown warnings do not appear fails on that defect: the row records `fail` on its warning clause, citing #523. That failure is expected by design and is disposed of at the gate as a defect outside this change, not as evidence against the item cleanup | Wait up to 15 seconds without running any `/clean` command | At the 10/5/3/2/1-second countdown marks, chat broadcasts the warning "[Cleaner] Ground items will be cleaned in T seconds!" (T the mark) (FIVE times — 60 and 30 are above the lowered 15-second interval and are never reached, but all of 10, 5, 3, 2 and 1 are); at 0 seconds, the eligible item(s) are removed and chat reads "[Cleaner] Cleaned N ground items!" (green); the countdown then restarts at 15 | server | |
| ulticleaner.scheduled.entity-tick | `language: en` in the framework's `config.yml`, and the seven `messages.*` keys in `config/cleaner.yml` holding the English text this build writes under `language: en` (a fresh file, or an upgraded file whose old Chinese defaults start-up has rewritten); `entity.enabled: true` (shipped default); `entity.interval: 15` (NOT the shipped default of 600 — lowered for testability); `entity.warn-times` at its shipped default; `item.interval` RESET back to its shipped default (300), or otherwise confirmed not to reach zero during this row's own 15-second wait, if `ulticleaner.scheduled.item-tick` above lowered it in this same session — a same-tick collision between item and entity cleanup silently drops one of them via the shared `isCleaningInProgress` flag, `UltiKits/UltiCleaner#15`; at least one eligible entity present (same eligibility as `ulticleaner.clean.entities` above); known framework defect outside this change, `UltiKits/UltiTools-Reborn#523` (module symptom `UltiKits/UltiCleaner#22`): the framework's config parser binds every element of `entity.warn-times` as a string while the module looks each countdown mark up as an integer, so on a boot that reads the key from an existing `config/cleaner.yml` no countdown warning is ever broadcast. Until #523 is fixed, a run in which the entity is removed at 0 seconds but the countdown warnings do not appear fails on that defect: the row records `fail` on its warning clause, citing #523. That failure is expected by design and is disposed of at the gate as a defect outside this change, not as evidence against the entity cleanup | Wait up to 15 seconds without running any `/clean` command | At the 10/5/3/2/1-second countdown marks (FIVE broadcasts total, by the same count as `ulticleaner.scheduled.item-tick` above), chat broadcasts "[Cleaner] Entities will be cleaned in T seconds!" (T the mark); at 0 seconds, the eligible entity is removed and (since the removed count is above zero) chat reads "[Cleaner] Cleaned N entities!" (green); the countdown then restarts at 15 | server | |
| ulticleaner.scheduled.smart-clean | `language: en` in the framework's `config.yml`, and the seven `messages.*` keys in `config/cleaner.yml` holding the English text this build writes under `language: en` (a fresh file, or an upgraded file whose old Chinese defaults start-up has rewritten); `smart.enabled: true` (NOT the shipped default — set it first); `smart.item-threshold: 100` (its `@Range` minimum, NOT the shipped default of 2000 — lowered for testability); at least 101 non-whitelisted, unnamed, aged dropped items present server-wide (in a non-blacklisted world) BEFORE this row starts | Wait up to 5 seconds without running any `/clean` command | Within 5 seconds, chat broadcasts "[Cleaner] Detected too many entities, initiating smart cleanup..." (yellow), followed by the same item-cleanup broadcast sequence as `ulticleaner.scheduled.item-tick` above (batch removal, "[Cleaner] Cleaned N ground items!"); the item count afterward is at or below `smart.item-threshold` | server | |
| ulticleaner.scheduled.tps-fallback | None (this repository's real-machine target is Paper 1.21.11) | Watch the server console and `/clean status`'s own TPS line for 5 seconds | No new console output attributable to `TpsAwareScheduler#updateFallbackTps` appears, and `/clean status`'s TPS reading is unaffected by whether this task ran — `ServerTypeUtil#hasTpsMethod` returns `true` on Paper, so `fallbackMonitorEnabled` is `false` and every invocation of this task returns immediately with no side effect; this task has no positive effect to observe on this repository's pinned real-machine environment, and this row's PASS criterion is that absence, not a positive log line | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `CleanerConfig`
(`config/cleaner.yml`, 33 keys), matching `FEATURES.md`'s `## Configuration` section exactly.
This row confirms every key is present at its documented default, confirms that the five keys this
version removed are absent from a freshly written file, then flips one representative
interval-style key and observes the behaviour follow.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ulticleaner.config.cleaner-yml | Fresh `config/cleaner.yml`, written by this version from its declared defaults (delete any existing file first, so the framework writes a new one); known framework defect outside this change, `UltiKits/UltiTools-Reborn#523` (module symptom `UltiKits/UltiCleaner#22`): the framework's config parser binds every element of `item.warn-times` as a string while the module looks each countdown mark up as an integer, so on a boot that reads the key from an existing `config/cleaner.yml` no countdown warning is ever broadcast. Until #523 is fixed, a run in which the item cleanup fires at the lowered interval but the countdown warnings do not appear fails on that defect: the row records `fail` on its warning clause, citing #523. That failure is expected by design and is disposed of at the gate as a defect outside this change, not as evidence against the interval key. This row's restart is exactly such a boot, and the interval claim is proven by the cleanup's cadence alone | Load the file; confirm all 33 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults, and that `messages.prefix`, `chunk.enabled`, `chunk.max-distance`, `chunk.batch-size` and `chunk.timeout` are absent; then RESTART THE SERVER COMPLETELY (a full restart rather than `/ul reload UltiCleaner`, so that this row proves the value is read at boot; the reload path is proven separately by the row under `## Lifecycle Hooks` at the end of this file) and set `item.interval: 15` (default 300) before the restart, then repeat `ulticleaner.scheduled.item-tick` above, confirming the item cleanup now fires around 15 seconds later rather than 300 | All 33 keys present at their documented defaults before the change, and none of the five removed keys written into the fresh file; after lowering `item.interval` to 15, the scheduled item cleanup fires (warning broadcasts, then removal) around 15 seconds later rather than 300, proving the lowered value took effect | server | |

## Lifecycle Hooks

Three rows. `ulticleaner.lifecycle.removed-key-warning` exercises the startup check that tells an
operator a key they still have in their own file is no longer read; its first step is its own
positive control, because a check that never fires and a file with no leftover keys produce the
same (empty) console. `ulticleaner.lifecycle.legacy-message-defaults` exercises the start-up step
that rewrites a broadcast message still holding the Chinese default an earlier version shipped in the
server's language; it observes the item-cleaned broadcast after `/clean items`, which
does not depend on the countdown warnings `UltiKits/UltiTools-Reborn#523` suppresses.

`ulticleaner.lifecycle.reload` exercises `UltiKits/UltiCleaner#14`'s lifecycle-hook migration: the
framework's `final`
`reloadSelf()` now re-reads `config/cleaner.yml` before it calls this module's `onReload()` hook,
and that hook resets both cleanup countdowns from the reloaded values (see `FEATURES.md`'s
`## Lifecycle Hooks`). Before the migration the module's own `reloadSelf()` override skipped the
file re-read, so the countdown was reset to the OLD interval. Its Expected is chosen so that
behaviour fails the row.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ulticleaner.lifecycle.removed-key-warning | `language: en` in the framework's `config.yml`; server stopped. In `config/cleaner.yml`, hand-add the five keys this version removed, exactly as an upgraded install would still carry them: a top-level `chunk:` block with `enabled: false`, `max-distance: 20`, `batch-size: 5`, `timeout: 5`, and a `prefix: '&a[清理]'` entry inside the existing `messages:` block | 1. Start the server and read the console. 2. Stop the server, delete all five keys from `config/cleaner.yml`, save. 3. Start the server again and read the console | Step 1 prints exactly five WARNING lines from this module, one per key, each containing the text `UltiCleaner`, the path of the `config/cleaner.yml` it read, and the key's own dotted name — `messages.prefix`, `chunk.enabled`, `chunk.max-distance`, `chunk.batch-size`, `chunk.timeout` — and each ending with an instruction to delete the key. The `messages.prefix` line says nothing ever read that key, and points the operator to the `messages.*` key of the broadcast they want to change, because a broadcast's prefix is part of that broadcast's own text. Under `language: en` the lines are English; they come from the language file, like every console line this module writes (`UltiKits/UltiCleaner#17`). The four `chunk.*` lines say there is no replacement setting. Step 3 prints NONE of those five lines. Step 1 is this row's positive control and must be read first: without it, step 3's silence is indistinguishable from a check that never fires (`UltiKits/UltiCleaner#18`, `UltiKits/UltiCleaner#23`) | server | |
| ulticleaner.lifecycle.legacy-message-defaults | `language: en` in the framework's `config.yml`; server stopped; `config/cleaner.yml` as an install upgraded from an earlier version carries it: every `messages.*` value at the Chinese default every earlier version shipped (the shared server's state), except `messages.entity-cleaned`, set to `&aGone: {COUNT}` (an operator's own value); a byte copy of the file saved outside the server with its SHA-256; at least one eligible dropped item present, with the same eligibility as `ulticleaner.clean.items`; no cleanup already in progress | 1. Start the server, then open `config/cleaner.yml` and read the two keys. 2. Run `/clean items` and wait for the batch to finish; read the chat. 3. Stop the server and start it again, then compare the whole file with its state after step 1. 4. Cleanup: stop the server and restore the byte copy (SHA-256 read back) | Step 1: `messages.item-cleaned` is now `'&a[Cleaner] &fCleaned &e{COUNT} &fground items!'`, `messages.warn` `'&c[Cleaner] &fGround items will be cleaned in &e{TIME} &fseconds!'` — every shipped default rewritten to the English text of `lang/en.yml` — while `messages.entity-cleaned` is still `&aGone: {COUNT}`; the file was saved. Step 2: chat shows "[Cleaner] Cleaned N ground items!" (N at least 1) — exactly the file's text. Step 3: the file is byte-identical to its state after step 1 (a second start with the same language writes nothing) | server | |
| ulticleaner.config.cleaner.materialize-fresh | `language: en` in the framework's `config.yml`; the server is stopped; `plugins/UltiTools/pluginConfig/UltiCleaner/config/cleaner.yml` and `plugins/UltiTools/pluginConfig/UltiCleaner/lang/` moved to a backup directory outside the server (SHA-256 of each recorded), so both are written fresh; at least one eligible dropped item present, with the same eligibility as `ulticleaner.clean.items`; no cleanup already in progress | 1. Start the server. 2. Read the seven `messages.*` keys in `plugins/UltiTools/pluginConfig/UltiCleaner/config/cleaner.yml`. 3. Run `/clean items` and wait for the batch to finish; read the chat. | Step 2: `messages.warn: '&c[Cleaner] &fGround items will be cleaned in &e{TIME} &fseconds!'`, `messages.item-cleaned: '&a[Cleaner] &fCleaned &e{COUNT} &fground items!'`, `messages.clean-progress: '&7[Cleaner] &fCleaning progress: &e{CURRENT}&f/&e{TOTAL}'` and the other four in English — no Chinese text in any of the seven. Step 3: "[Cleaner] Cleaned N ground items!" (N at least 1) — exactly the file's text. Leave the server running and the file as written: `ulticleaner.config.cleaner.materialize-switch` continues from this state | server | |
| ulticleaner.config.cleaner.materialize-switch | `ulticleaner.config.cleaner.materialize-fresh` passed and `plugins/UltiTools/pluginConfig/UltiCleaner/config/cleaner.yml` still holds the English text it wrote, unedited, except `messages.entity-cleaned: '&aGone: {COUNT}'` set by hand; the server is still running with `language: en` (run this row straight after `ulticleaner.config.cleaner.materialize-fresh`, before its backups are restored); `plugins/UltiTools/config.yml` backed up with its SHA-256 | 1. Set `language: zh` in `plugins/UltiTools/config.yml`, save, and run bare `ul reload` from the console (not `ul reload UltiCleaner`, which does not re-read `language`). 2. Read `messages.item-cleaned` and `messages.entity-cleaned`. 3. Set `language: en`, save, run bare `ul reload` again, and read the two keys. 4. Cleanup: stop the server; restore `plugins/UltiTools/config.yml` from its backup and `config/cleaner.yml` and `lang/` from the backups `ulticleaner.config.cleaner.materialize-fresh` took (SHA-256 of each read back). | Step 2: `messages.item-cleaned` is byte-identical to `item_cleaned` of the jar's `lang/zh.yml` (the Chinese text earlier versions shipped), and `messages.entity-cleaned` is still `&aGone: {COUNT}`. Step 3: `messages.item-cleaned` is `'&a[Cleaner] &fCleaned &e{COUNT} &fground items!'` again and `messages.entity-cleaned` is still `&aGone: {COUNT}` — an untouched value follows the switch in both directions, an edited one never does | server | |
| ulticleaner.lifecycle.reload | `language: en` in the framework's `config.yml`; Sender is an operator (`/ul` is `requireOp = true`) and holds `ulticleaner.clean`; `item.enabled: true` and `item.interval: 300` (both shipped defaults) in `config/cleaner.yml` AS CURRENTLY LOADED — earlier rows lower `item.interval`, so set it back to 300 and then either restart or run `/ul reload UltiCleaner`; `entity.interval` at its shipped default (600); `smart.enabled: false` (shipped default) | 1. Run `/clean status` and note S1, the "Next item cleanup: S1 seconds" value. If S1 is 40 or less, wait until the countdown passes zero and restarts, then run `/clean status` once more; if S1 is still 40 or less, the loaded interval is not 300 and the precondition is not met — record `blocked`, do not keep waiting. 2. Without restarting the server, edit `config/cleaner.yml` to `item.interval: 20` and save. 3. Run `/ul reload UltiCleaner`. 4. Immediately run `/clean status` and note S2. 5. Wait 25 seconds without running any `/clean` command, then run `/clean status` and note S3. 6. Cleanup: set `item.interval` back to 300, save, and run `/ul reload UltiCleaner` again | After step 3 the sender receives (gloss) "Module UltiCleaner has been reloaded" (green), and the console shows the framework's own line (gloss) "Module 'UltiCleaner' reloaded." BEFORE this module's own "UltiCleaner configuration has been reloaded!" line. S2 is 20 or less: the countdown was reset to the new interval, not left running down from S1 and not reset to 300. S3 is 20 or less: the countdown reached zero within about 20 seconds of the reload and restarted from the new interval of 20, not from 300. A value of S2 or S3 above 20 (in particular one near 300) is a fail. The entity countdown is also reset by the reload, to 600, so no entity cleanup can collide with the item cleanup during the wait (`UltiKits/UltiCleaner#15`) | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ulticleaner.i18n.language | `language: en` in the framework's `plugins/UltiTools/config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiCleaner/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459); no cleanup batch running (so `/clean status` reports the idle state) | From the server console run `/clean` and then `/clean status` | `/clean` prints these six lines: `=== UltiCleaner Help ===`, `/clean items - Clean ground items`, `/clean entities - Clean entities`, `/clean all - Clean everything`, `/clean check - View entity statistics`, `/clean status - View cleanup status`; `/clean status` prints `=== Cleanup Status ===`, `Next item cleanup: N seconds`, `Next entity cleanup: N seconds`, `Cleanup status: Idle`, and a `TPS: ` line. None of the lines these two commands printed contains Chinese text (colour codes rendered or stripped by the console, text otherwise exact) | server | |
